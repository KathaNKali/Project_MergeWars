namespace MergeWars.Heroes
{
    // TODO(design): scaffolded only, not implemented this pass. Identity +
    // unique traits layer of the character architecture (see
    // /GameDocs/Systems/SYS_HeroDefinition.md). No real species content/rules
    // exist yet — this is a placeholder so HeroDefinition has a field to
    // reference once this layer is designed.
    [System.Serializable]
    public class HeroSpecies
    {
        public string id;
    }
}
